#include <bits/stdc++.h>
using namespace std;

#define FAST_IO                  \
    ios::sync_with_stdio(false); \
    cin.tie(0);                  \
    cout.tie(0);

#define ll long long
#define ld long double
#define pb push_back
#define all(x) x.begin(), x.end()
#define sz(x) ((int)(x).size())

const int MOD = 1e9 + 7;
const int INF = 1e9 + 5;
const ll LINF = 1e18;

void solve()
{
    int A, B;
    cin >> A >> B;

    int totalLines = A * B * 100;

    cout << totalLines << endl;
}

int main()
{
    FAST_IO;

    int T = 1;

    while (T--)
    {
        solve();
    }

    return 0;
}